class Solution:
    def majorityElement(self,nums):
        n=len(nums)
        for i in nums:
            if(i in d):
                d[i]=d[i]+1
            else:
                d[i]=1
        for i,j in d.items():
            if(j>n//2):
                return i
ob=Solution()
nums=[1,2,2,2,3,]
print(ob.majorityElement(nums))

output:2         
